# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Local env / secrets
.env
.env.*

# Agent guidance — kept local, not published
CLAUDE.md

# Data artifacts (Maia checkpoints download on first use, etc.)
data/*
!data/.gitkeep

# Model checkpoints (Allie weights: ~1 GB small, ~3.7 GB medium — downloaded, not committed)
models/
*.pt
